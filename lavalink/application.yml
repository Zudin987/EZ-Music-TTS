server:
  port: 2333
  # Native Windows setup: never expose Lavalink to the LAN.
  address: 127.0.0.1

lavalink:
  plugins:
    - dependency: "dev.lavalink.youtube:youtube-plugin:1.18.2"
      snapshot: false
  server:
    password: "ezmusic-local-only"
    sources:
      youtube: false
      bandcamp: true
      soundcloud: true
      twitch: false
      vimeo: false
      nico: false
      http: true
      local: false
    # Raw-song policy: volume is the only enabled audio control.
    filters:
      volume: true
      equalizer: false
      karaoke: false
      timescale: false
      tremolo: false
      vibrato: false
      distortion: false
      rotation: false
      channelMix: false
      lowPass: false

plugins:
  youtube:
    enabled: true
    allowSearch: true
    allowDirectVideoIds: true
    allowDirectPlaylistIds: true
    # MUSIC is search-only. The remaining clients are anonymous playback
    # fallbacks, ordered to try newer/less restricted clients before WEB.
    clients:
      - MUSIC
      - TVHTML5_SIMPLY
      - ANDROID_MUSIC
      - ANDROID_VR
      - MWEB
      - WEB
      - WEBEMBEDDED

logging:
  level:
    root: INFO
    lavalink: INFO
